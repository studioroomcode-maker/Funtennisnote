# 재미있는 테니스 노트 EP01 — Codex 작업 규칙

## 현재 기본 작업 — V2 제작 대시보드

- 기본 기획은 `docs/FTN_EP01_yellow_ball_78s_prompt_package.md`의 78초·18컷 버전이다.
- `dashboard/`는 단계별 자동·검토·수동 모드와 Flow/Higgsfield 선택을 관리한다.
- 기본 생성 엔진은 사용자가 구독 중인 Google Flow이며, 전체 또는 컷별로 Higgsfield를 선택할 수 있다.
- 외부 서비스 API 키·토큰·로그인 정보는 대시보드나 저장소에 기록하지 않는다.
- 연결 어댑터가 없으면 외부 생성 단계는 작업 대기열 JSON을 만든 뒤 `waiting_external` 상태에서 멈춘다.
- 새 78초 제작에서는 테도리를 기본적으로 등장시키지 않는다. CUT 01·11·14·18에서도 선택 요소이며 가독성을 해치면 완전히 생략한다.
- 대시보드는 `powershell -ExecutionPolicy Bypass -File .\dashboard\start_dashboard.ps1`로 실행한다.

## 레거시 43초 제작 목표

아래 규칙은 기존 `episode_manifest.json` 기반 7컷 영상을 다시 제작할 때만 적용한다. V2 대시보드와 18컷 작업에는 적용하지 않는다.

- 승인된 7개 키프레임을 Higgsfield 이미지→영상 생성으로 애니메이션화한다.
- 장면을 순서대로 편집해 9:16, 1080×1920, 30fps, 약 43초 H.264 MP4를 만든다.
- 최종 파일명은 `output/FTN_EP01_yellow_tennis_ball_no_subtitles.mp4`로 한다.

## 절대 조건

- 최종 영상에 자막, 제목, 연도 글자 오버레이, 워터마크, 로고, UI, 생성된 임의 문자를 넣지 않는다.
- Higgsfield의 생성 음성·배경음·효과음을 끈다. 사운드는 후속 단계에서 별도로 제작한다.
- 키프레임을 새 이미지로 다시 만들지 않는다. `assets/scene_*.png`를 시작 프레임으로 사용한다.
- 테돌이의 구형 몸, 좌우 대칭 눈 간격, 작은 입, 흰 곡선 솔기 두 줄, 짧은 팔다리를 유지한다.
- 테니스공 솔기를 축구공·야구공 무늬로 바꾸지 않는다.
- 인물, 브랜드, 대회 로고, 라켓을 임의로 추가하지 않는다.
- 이 에피소드에서 테스노트 기능을 설명하거나 설치를 유도하지 않는다.
- 사실을 추가하거나 각색하지 않는다. 역사적 사실은 `docs/production_package.md`와 ITF 공식 자료만 사용한다.
- API 키·토큰·로그인 정보는 저장소에 쓰거나 출력하지 않는다. 환경변수 또는 연결된 플러그인만 사용한다.

## 작업 방식

1. 시작 전에 `CODEX_TASK.md`, `episode_manifest.json`, `docs/production_package.md`를 읽는다.
2. Higgsfield 연결 상태, 활성 워크스페이스, 크레딧을 읽기 전용으로 확인한다.
3. 생성 전 모델·해상도·장면별 예상 크레딧과 총액을 기록한다. 사용자가 이미 1,200크레딧 보유를 알렸으나 실제 조회값을 기준으로 한다.
4. 사용자 승인 없이 키프레임이나 대본의 의미를 바꾸지 않는다.
5. 장면별 작업 ID, 모델, 프롬프트, 비용, 결과 URL을 `output/generation_manifest.json`에 기록한다.
6. 실패한 장면만 재생성한다. 성공한 장면을 무조건 다시 생성해 크레딧을 낭비하지 않는다.
7. 모든 클립을 다운로드한 뒤 `ffprobe`로 길이·해상도·프레임레이트를 검사하고 FFmpeg로 편집한다.
8. 최종 영상에서 7개 장면 순서, 테돌이 일관성, 공 모양, 글자 생성 여부를 프레임 샘플로 검수한다.
9. 결과와 실제 사용 크레딧, 재생성 장면, 남은 문제를 짧게 보고한다.

## 모델 선택

- 1순위: Seedance 2.0 또는 현재 Higgsfield에서 이미지 정체성 보존이 더 좋은 최신 Seedance 계열.
- 초안 비용 절감이 필요하면 Seedance 2.0 Mini 720p를 사용할 수 있다.
- 모델을 임의로 바꾸기 전 `estimate_video_cost` 성격의 읽기 전용 비용 조회를 수행한다.
- 각 장면은 단일 연속 숏이다. 컷, 숨은 컷, 카메라 리셋, 캐릭터 재등장을 금지한다.

## 완료 기준

- 7개 장면 모두 승인된 시작 이미지와 동일한 디자인에서 출발한다.
- 전체 영상은 약 43초이며 영상 스트림과 무음 오디오 스트림의 길이가 동일하다.
- 화면에 새로 생성된 자막·로고·텍스트가 없다.
- 출력은 모바일 쇼츠에서 바로 재생 가능한 H.264/AAC, yuv420p, faststart MP4다.
